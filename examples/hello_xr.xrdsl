; Hello XR - Basic DSL example
; This creates a simple pulsing panel in XR space

(defbehavior hello
  (state (t 0.0))
  (update (dt)
    (set! t (+ t dt))
    (let ((intensity (+ 0.5 (* 0.5 (sin t)))))
      (gpu.panel.color intensity 0.8 1.0 1.0))))