(defbehavior spin
  (state (speed 1.0))
  (update (dt)
    (set! rotation.y (+ rotation.y (* speed dt)))))

(defscene3d demo-scene
  (camera
    (position 0 8 15)
    (target 0 0 0)
    (fov 40))
    
  (object cube1 cube
    (position 0 1 0)
    (scale 1.0 1.0 1.0)
    (behavior spin)))