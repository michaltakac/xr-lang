; Simple test for behavior hot-swapping

(defbehavior test-spin
  (state (speed 1.0))
  (update (dt)
    (set! rotation.y (+ rotation.y (* speed dt)))))

(scene3d test-scene
  (objects
    (cube test-cube
      (transform
        (position 0.0 0.0 0.0))
      (behavior test-spin))))